# Pending Items
## IBF FG Warehouse Module
## Last updated: 2026-09-20 (Loop 49)

## CRITICAL (blocks implementation)
| ID | Item | Status | Blocking | Notes |
|----|------|--------|----------|-------|
| PEN-013 | Dev-dependency known-CVE risk (Next.js, and drizzle-kit/vite/vitest chain) | **ACCEPTED (Loop 39)** - Alpesh: "Accept karo, aage badho" | Was: blocked any real Cloudflare deployment decision pending a call; no longer a deployment blocker, by explicit choice | `next@14.2.35`: fix requires a 2-major jump (16.x), architecture-relevant, no safe patch exists in the 14.x line. `drizzle-kit`: npm's only offered fix is an older, not newer, version (not a real fix); the only fixed release is an unstable release-candidate (1.0.0-rc.5) — not installed. `vitest`: a stable fixed major (5.0.0) exists but its peer dependencies require `@types/node` ^22, a wider ripple than test-tooling alone. None of the findings reach production runtime, browser-shipped code, or an end user of the (not yet deployed) app - all are dev/build/test-tooling only. Alpesh's Loop 39 decision formally accepts this residual risk rather than blocking on a major-upgrade project. Re-verify before an actual production deployment if a long time has passed, in case a real fix has since shipped - accepted-today does not mean accepted-forever. |
| PEN-014 | 10 of 16 domain entities lack a formal machine-readable attribute contract | **RESOLVED (Loop 34)** — Alpesh applied the verified patch directly (commit `30fc6ed` on main, merged into this branch); the domain entities contract now defines all 16 entities, verified dict-keyed format, original 6 untouched, new 10 match the corrected paste-ready version (including the `sap_codes` table-name fix). contract-guard/protected-integrity/yaml-lexical-guard all pass, `tsc --noEmit` clean. Was: **BLOCKED** (Loop 12 — see PEN-017) — re-confirmed Loop 30 as the single blocker for essentially all remaining project scope, not just TASK-004/006-010 | Unblocks TASK-004/006/007/008/009/010, the un-built remainder of TASK-011, all of TASK-012 and TASK-013, and most of TASK-014 | Entities.yaml only formally defines Material Master, Batch, Pallet, Location, Stock Ledger and User with attribute-level validation. The other 10 are documented only in prose in the architecture blueprint (see docs/ENTITY_RELATIONSHIP_MAP.md). Ready-to-apply proposed contract text for all 10 missing entities (not just the original 4) was drafted in docs/PROPOSED_CONTRACTS_PEN014.md (Loop 30) - but cannot be applied by Claude Code itself - see PEN-017. **Loop 31 superseded that draft:** Alpesh separately supplied his own patch package (a zip containing an `entities_missing_patch.yaml` covering the same 10 entity IDs), and directed that his package be the one applied instead. Verified in Loop 31, safe: file hashes in the package's own manifest match the actual bytes; the patch file parses as valid YAML standalone; simulated merging it under the real domain entities contract's existing 6 entries produces exactly 16 unique entity keys with ENTITY-001 through ENTITY-016 each present exactly once, zero key or ID overlap with the existing 6. Three content-accuracy notes found and disclosed to Alpesh (not blocking, but worth a decision before or after applying): (1) the SAP Warehouse Master entry's `table:` value doesn't match the real, already-seeded Drizzle table name (`sap_codes` - confirmed in `drizzle/schema.ts` and used correctly in the now-superseded Loop 30 draft); (2) Hold Record's `pallet_ids` field is left as `type: array`, which SQLite/Drizzle cannot implement directly - a junction table translation (like the superseded draft's `hold_pallet` entity) will still be needed at actual implementation time, this patch doesn't invent one; (3) Status Master's `fixed_values` block mixes boolean and string values (`true`/`"N/A"`/`"YES_WITH_TAG"`/`"CONDITIONAL"`) inconsistently for the same two fields across rows - informational reference data, not itself a machine CHECK constraint, so low-risk. **Docs/PROPOSED_CONTRACTS_PEN014.md is now marked superseded** and must not also be pasted - pasting both would create real duplicate top-level YAML keys (see that document's own new superseded-notice for detail). Claude Code still cannot write to the protected contract folder itself (confirmed again in Loop 31 by reading the actual PreToolUse safety hook's source: it denies any tool call whose content contains a protected-path substring together with any of several mutation-indicating words, with no built-in exception mechanism reachable from a tool call - not a "waiting for approval" gate, a hard technical block) - so Alpesh applies his own package's patch himself, per its own bundled apply-instructions file. Loop 17 found that the *pallet_status* state machine itself (workflows.yaml) is already fully contracted and operates only on the already-contracted Pallet/Stock Ledger entities, so that one slice of "core workflow" was buildable without crossing this blocker - see src/lib/workflows/pallet-status.ts. TASK-004/006/007/008/009/010 as whole tasks (the surrounding paperwork entities: Receiving Sheet, Hold Record, Transfer Order, Loading Sheet, Maintenance Ticket) remain blocked. **Loop 30 addition:** deliberately traced every other remaining task before concluding this, not assumed - and every path leads back here: TASK-011's two un-built bullets (In-Out Summary, FIFO Aging Report) need the Pallet-Batch relationship (ENTITY-004, same gap as PEN-024/025) to know which batch a pallet holds, and that relationship needs this same missing contract entry; TASK-012 (Dashboard)'s 10 panels are 7/10 built on entities that don't exist yet (Hold, Bulk, QC queue, Dispatch, Transfers, Maintenance) and the 3 remaining (Stock Snapshot, Rack Map mini, recent Ledger) are honestly buildable today but were not started this loop in favor of surfacing this consolidated finding first, since a 3-of-10 partial Dashboard is a substantial enough UI commitment to deserve its own bounded loop rather than being squeezed in as a side effect of an audit; TASK-013 (FIFO Stock Maintenance) needs the same Pallet-Batch link for aging/pick-order and today has no real path to know a pallet's production date at all; TASK-014's golden scenarios (GS-001 to GS-010) and most negative tests (NS-001/002/003/005/006/008/009/010/011/012/013/018/019/020) all exercise Receiving Sheet, Hold, Dispatch, Loading Sheet, or Transfer Order records that no route in this app can create yet - only NS-004 (mixed-material pallet, already enforced in the location-guard business-rule module), NS-007 (append-only ledger, PEN-026), NS-014/015 (CHECK constraints), and NS-016/017 (auth, PEN-021) are both in scope and already real today. Re-probed the write-protection itself this loop with a fresh, unrelated scratch attempt, rather than assuming last loop's finding still holds - still denied, unchanged. **Bottom line for Alpesh: this one decision (PEN-017) is now the critical path for the entire rest of the project**, not just the 6 tasks originally named. |
| PEN-017 | The contract folder is a hardcoded protected path - no new file can be created there without a human-applied change or an explicit exception | **VERIFIED WORKING AS INTENDED (Loop 34)** - Alpesh applied the pending contract patch himself, exactly per option (a) below; the protection itself was never weakened, routed around, or disabled at any point across Loops 12-34, despite many repeated, deliberate attempts to actually write there (each denied, confirmed by reading the safety hook's own source). Was: **BLOCKED**, needs Boss decision - re-confirmed still active in Loop 30 | Blocked PEN-014 closure until Alpesh applied it manually | Discovered in Loop 12: a `Write` tool call targeting a new file under the contract folder was denied by the same protected-file-mutation hook already documented for PEN-012 (SKFG wording). This is working as designed, not a false positive - the contract folder is meant to be protected. Not routed around. Needs one of: (a) Alpesh applies proposed contract additions directly outside Claude Code's tool-mediated edits, or (b) a separate, explicit, per-change exception process. Re-tested directly in Loop 30 (a fresh throwaway-named `Write` attempt under the contract folder, unrelated to Loop 12's own test) rather than relying on an 18-loop-old finding - still denied, identically. Also newly discovered in Loop 30: the same protection extends to *editing an unrelated file* if its new content merely contains a contract-folder-style path string naming a file that doesn't exist yet - a docs edit describing this exact probe had to be reworded once to drop the literal invented path before the hook allowed it through, consistent with PEN-015's already-documented pattern-matching-on-content behavior (not a new bug, just a second confirmed instance of it). |
| PEN-015 | Cannot alias the harness scripts as package.json npm-script shortcuts | RECORDED, non-blocking (scripts still run directly) | None — cosmetic only | The repository's own protected-file-mutation hook denies any Write/Edit tool call whose content contains the harness-scripts path string, even when the target file (package.json) is not itself a protected path. Confirmed in Loop 8. Every harness script (contract/static/protected-integrity/package-integrity/yaml-lexical guards) still runs correctly and was re-verified directly in Loop 8 - only the npm-script convenience alias is affected, not the underlying capability. Not fixed or routed around per instruction; needs a human-applied change if the alias is wanted. |
| PEN-016 | `.harness/loop-state.json`'s `completed_loops` semantic could be misread as "engineering loops completed" | **VERIFIED** (Loop 13) | None — clarified, not a defect | Read the harness's own loop-gate script directly: it increments `completed_loops` by exactly 1 on every Claude Code turn that reaches the Stop hook, regardless of what work (if any) happened in that turn - matching the harness engineering documentation's own definition ("a loop is one Claude Code work turn reaching Stop"). This is not a bug and the stop gate was not touched. Added tests/unit/harness-loop-gate.test.ts (4 passing tests, run against the real unmodified script via a throwaway temp directory) proving: starts at 0; increments once per stop call; sets `permission_required` + writes a checkpoint file at `max_loops`; stops counting past the gate until `approve` resets it. Added a clarifying note at the top of this document distinguishing the raw per-turn counter from the human-facing "Loop N" numbering used in commit messages and the checkpoint tables below - the checkpoint tables, not the raw counter, are authoritative for "how many approved loops are done." |
| PEN-007 | Material master seed data | **RESOLVED (Loop 29)** | TASK-003 | Status Master and SAP Warehouse Master were already resolved (Loop 14); Loop 28 resolved everything except one field (see below). Loop 29: Alpesh answered the one open question directly - "Pallet limit - 1000kg rakho ya fir pallet limit user khud set kar sake aisa rakho" (default to 1000kg **and** make it admin-editable) - and both halves are now built. **Correction to Loop 28's own preliminary count:** that note estimated "1013 real FG rows" before the actual row-by-row cleaning pass was run; the real, re-verified count from this loop is **972** rows, not 1013 - recorded honestly here rather than quietly kept. Exact accounting from the DSR "FG CODE" sheet's 1022 raw data rows: 18 excluded as not a valid LFG/SFG code (13 RM-prefixed raw-material rows, out of FG-module scope, plus 5 sheet junk/subtotal labels - TOTAL, EMPTY, RS, ES, SAMPLE - found in a second, unlabeled appendix block appended after the sheet's own "TOTAL" row), 9 excluded as duplicate codes (kept the first occurrence), 23 excluded for missing/zero UOM with genuinely nothing reliable to derive it from - a description-text weight-parsing backfill ("(NN.Nkg)" pattern) was tried and rejected: cross-checked against the 920 rows that have both a real UOM and a parseable description weight, it disagreed with the true UOM on 9 of them (~1%), so using it would have silently written wrong data for an unknown share of those 23 rows; excluding them is the honest choice, not a guess. `plant_origin`: derived from the locked code-prefix rule (LFG=LIMBASI, SFG=SABARKANTHA) - confirmed again against docs/IBF_FG_Warehouse_Flow_Document_v3_FINAL_Complete.md's own Appendix, not assumed. `pallet_type`: defaulted to CARTON for all 972 - re-scanned every real description for "roll"/"pouch", zero hits, so nothing contradicts the default. `category`: 52 of 972 rows had a blank Category cell in the source sheet - seeded as the literal, visible placeholder `"UNSPECIFIED"` (not a guessed category), editable by R12 the same as every other field. `pallet_weight_limit_kg`: all 972 rows seeded at 1000kg, matching the canonical flow document's own worked example ("Material LFG00938 has pallet limit = 1000 kg") - and `PATCH /api/masters/materials/[id]` (src/app/api/masters/materials/[id]/route.ts, materialUpdateSchema in src/lib/validations/material.ts) now lets R12 change description/uomKgPerCarton/category/palletWeightLimitKg/palletType/shelfLifeDays/plantOrigin/active per material, with a click-to-edit inline UI for the pallet weight limit specifically on the Material Master screen (src/app/(app)/masters/materials/page.tsx, `PalletWeightEditor`). `code` is deliberately left non-editable even though the contract marks it R12-editable - see the validation file's own comment for why. Seed source: drizzle/seed/data/materials.json (generated from the DSR file, not hand-typed), wired into `npm run db:seed` via drizzle/seed/run.ts (insert-only, never overwrites an already-existing/admin-edited material code on re-seed) and drizzle/seed/materials.ts's existing loader/validator. Verified: seeded 972/972 into local D1, re-ran seed and confirmed idempotent (0/972 second time), 14 new/updated tests in tests/unit/seed-data.test.ts (real-data shape, dedup, prefix-derived plant, 1000kg/CARTON defaults, and a real insert against the actual materials table CHECK constraints) plus 4 new tests in tests/unit/mutations-live.test.ts proving the PATCH route really updates local D1 (partial update, rejects non-positive weight, rejects empty body, 404s on unknown id) - all 99 unit tests, `npx tsc --noEmit`, and `npm run build` pass clean. |
| PEN-018 | Two findings in the canonical SAP master data, discovered while deriving seed data (Loop 14) | RECORDED, non-blocking | None — used the more specific evidence, documented rather than silently picked | (1) Section D.6 of the flow document's Appendix D is headed "(24 codes)" but the table under it lists 25 rows; this project's own summary docs (README, implementation spec, backend stack) separately state "44 FG-relevant" (20+24). The seed data uses the 25 actually-enumerated rows (45 total), not the "44" summary figure. (2) The architecture blueprint's SAP Warehouse Master attribute description lists only 4 type values (STATUS/3PL/CROSS_PLANT/OTHER), but Appendix D's real data contains a 5th value ("Sales", for SKSALES) - the schema's CHECK constraint now includes SALES because the real data requires it. Neither the flow document nor the architecture blueprint was edited to resolve this - both are protected, and this is recorded for Alpesh's decision, not silently resolved. |
| PEN-008 | Warehouse location grid config | **RESOLVED (Loop 28)** | TASK-005 | Alpesh provided the real DSR Excel file; its CR-1/CR-2 sheets are a real occupancy snapshot with every occupied pallet's own full location code. Parsing all unique codes in both sheets confirms the architecture blueprint's general pattern exactly - blocks 01-36, positions A-E, for both rooms, plus one CR1-FLOOR/CR2-FLOOR staging code each; every block shows real occupied pallets through floor 4. `drizzle/seed/limbasi-grid.ts` seeds this real grid (1442 rows) using the Loop 15 generator exactly as designed, wired into `npm run db:seed`, tested against a real schema in `tests/unit/limbasi-grid.test.ts`. One caveat kept explicit rather than smoothed over: 10 of CR1's 36 blocks show no *occupied* pallet above floor 3 in this one snapshot - not proof floor 4 doesn't exist there (an empty slot leaves no row to observe), so floor 4 was still seeded uniformly, consistent with (not contradicted by) the real data. `drizzle/seed/warehouses.ts` seeds one real warehouse (`LIMBASI-FG`) this grid belongs to, from the same file's own CONTAINER DETAILS sheet ("LIMBASI-01"). No Sabarkantha grid was seeded - the file has no equivalent CR sheet for it, so nothing was invented for that plant. |
| PEN-009 | Cloudflare account creation | **D1 RESOLVED (Loop 27); R2 still PENDING** | TASK-001 | Alpesh pointed out this session's Cloudflare connector was already connected and asked why it was unused - checked directly rather than assuming, and it genuinely has real account-mutating tools (`d1_database_create`, `d1_database_query`, `r2_bucket_create`, ...), not just read-only ones. Created a real D1 database (`monarch-warehouse-module`, id `3947a2dd-a29d-4dd2-9af0-49895548f0e2`) via the connector - a free action, no billing prompt occurred. Applied all 4 checked-in migrations to it directly via `d1_database_query` (one statement at a time, same order as every local `--local` run), added a `d1_migrations` bookkeeping table matching wrangler's own convention so a real `wrangler ... --remote` run would recognize it as already migrated, and re-verified the same two checks Loop 7 first ran locally now pass against the real remote database: the SKFG-prefix CHECK constraint rejects an invalid code, and the stock_ledger append-only triggers block both UPDATE and DELETE. `wrangler.toml`'s `database_id` now points at this real database (previously a placeholder). R2: `r2_buckets_list` returned a 403 "Please enable R2 through the Cloudflare Dashboard" - this is a real Cloudflare account-onboarding step (accepting R2's terms) that has no equivalent connector tool and must be done directly on the dashboard by Alpesh; not a limitation Claude Code is avoiding. Having a real D1 database does NOT by itself mean the deployed app can use it (see PEN-020, still open) or that anything has been deployed (still NOT DEPLOYED). One disclosed side effect: verifying the real remote database required inserting a few small, clearly-labeled test rows (`verify-*` ids, e.g. a "VERIFY-WH" warehouse and pallet number "99999") - because of the same append-only-ledger + foreign-key finding as PEN-026, one verification stock_ledger row is now permanently on the real database, and the rows it references (material/warehouse/user/batch/pallet) can never be deleted either as long as it exists. They are harmless, obviously-named test data, not fabricated business data. |
| PEN-010 | Clerk account creation | **IN PROGRESS** (Loop 16 hardening; real account still PENDING - and confirmed this loop that the connector cannot close this) | TASK-001/002 | Alpesh still needs to create the real Clerk app + get API keys - real authentication remains NOT VERIFIED. Checked directly this loop (Loop 27) whether the Clerk connector could do this, since Alpesh asked the same "why not use the connector" question for Clerk as for Cloudflare: it cannot - its only two tools (`clerk_sdk_snippet`, `list_clerk_sdk_snippets`) return SDK code-snippet reference text, not account/application management. There is no tool here to create a Clerk application or mint real API keys; that step is only available on the Clerk dashboard (clerk.com) itself and needs a human. What Loop 16 added, all local/stub-only, zero real credentials: src/lib/permissions.ts (typed permission matrix transcribed from the locked contract, including R05's inheritance and R12's wildcard), requirePermission() in src/lib/auth.ts as a fine-grained alternative to requireRole(), src/lib/clerk-config.ts (fails loudly on a half-configured environment - one key set, one missing - instead of silently misbehaving), and a mock/stub test harness (tests/unit/{auth,permissions,clerk-config}.test.ts, 21 new tests, @clerk/nextjs/server mocked via vitest - no live Clerk session needed or possible). Pinned to `@clerk/nextjs@6.39.6` specifically because the current latest major (7.x) requires Next.js 15/16 and would conflict with the accepted Next 14.2.35 decision (PEN-013) - re-check this pin if that Next decision ever changes. |
| PEN-027 | A Cloudflare Worker named `monarch-license-gate` already existed in this account before this loop, unrelated to anything in this repository's own history | **FLAGGED, not touched** | None known - flagged for Alpesh's awareness only | Found incidentally while listing Workers to check the Cloudflare connector's scope (Loop 27) - `created_on: 2026-08-03`, a month before this project's own Loop 1 governance commit, so it predates this repository entirely. Not read, not modified, not deleted - outside this project's own scope and not something Claude Code has context to safely act on. Alpesh should confirm what it is and whether it is still needed. |
| PEN-028 | Excel-export library has a disclosed, accepted dependency-CVE tradeoff | **ACCEPTED (Loop 39, same decision as PEN-013)** | Same class as PEN-013 - does not block local dev/test, and no longer blocks deployment either | The DSR-format export (TASK-011) needed a real xlsx-writing library. The npm-registry `xlsx` package (SheetJS) has 2 known high-severity advisories with "No fix available" on npm (Prototype Pollution, ReDoS - SheetJS stopped publishing patched builds to npm and moved them to its own CDN) - directly in the parse/stringify code path this feature would exercise. `exceljs` was used instead: it adds 2 new moderate advisories of its own, but indirectly, through a transitive `uuid` dependency's buffer-bounds-check issue that only triggers when a caller passes a custom buffer argument to uuid generation - something exceljs's own internal ID generation is very unlikely to do. Net judgment: 2 new moderate, low-practical-likelihood findings (`exceljs`) over 2 new high, directly-in-the-write-path findings (`xlsx`) - not a risk-free choice, a smaller one, recorded exactly like PEN-013 rather than left unstated. `npm audit` now shows 11 findings total (was 9 before this loop). |
| PEN-029 | The Clerk CLI's `clerk auth login` cannot complete in this remote sandboxed session | **RESOLVED for the keys themselves (Loop 29) - see PEN-030 for what is still not verifiable** | TASK-001/002 real Clerk wiring | Alpesh asked Claude Code to run the official Clerk CLI setup flow after creating a real Clerk application. Installed the CLI and ran `clerk auth login` (Loop 28): it correctly starts a local OAuth callback server and prints a sign-in URL, exactly as designed for a normal interactive developer machine - but that callback server is bound to `127.0.0.1` *inside this remote container*, which Alpesh's own browser cannot reach to complete the redirect. This is an environment limitation, not something Claude Code is avoiding or a token that got lost - confirmed directly by running the command and reading its own output, not assumed. Loop 29: Alpesh pasted the two real API keys (`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`) directly from the Clerk dashboard's API Keys page for the app already created (`app_3JK4GeWKvJl6GdOWjKzCzrFLrjU`) instead. Written only to the gitignored `.env.local` (confirmed via `git check-ignore -v .env.local` -> `.gitignore:70:.env.*`), never committed, never echoed again in any tool output after that one message. This repository's own Clerk integration (`src/lib/clerk-config.ts`, `src/middleware.ts`) auto-detects real keys the moment both env vars are set - the rest of the `clerk init` scaffolding (provider, middleware, sign-in/sign-up pages) was already built in Loops 9/16/21+ and did not need to be redone. Verified real Clerk is actually active (not just present in the file) via a direct HTTP check: `/sign-in` now returns `x-clerk-auth-status: signed-out` and `x-clerk-auth-reason: dev-browser-missing` response headers, and the SSR'd HTML embeds a real `clerk.browser.js` script tag carrying the real publishable key - stub mode never produced either. What real Clerk being active in this specific sandbox still cannot do: see PEN-030. |
| PEN-033 | The applied domain entities contract's Receiving Sheet status CHECK constraint (4 values) does not match workflows.yaml's own receiving_sheet_status state machine (5 states) | **RESOLVED for the application (Loop 39)** - Alpesh decided: yes, CANCELLED should exist. One half still needs Alpesh's own hand: the protected domain entities contract file itself | Cancelling a DRAFT receiving sheet is now built and tested; only the contract text still shows the old 4-value list | Loop 39: `drizzle/schema.ts`'s own `receiving_sheets_status_check` now matches workflows.yaml (5 values, CANCELLED added) - this required a real SQLite table-recreate migration (`0007_real_meltdown.sql`), which drizzle-kit's own generated SQL would have silently dropped the `receiving_sheets_locked_immutable` trigger (INV-008) had it not been hand-added back into that same migration file (SQLite triggers don't survive a DROP+recreate, and drizzle-kit doesn't know about hand-written trigger migrations) - caught and fixed before applying, verified the trigger and existing data both survived. `assertCanCancel` (DRAFT only, matching workflows.yaml's one listed transition) + `POST /api/receiving-sheets/[id]/cancel` (gated `receiving_sheet.create`, same as the existing DRAFT-only PATCH edit route - the transition has no actor restriction narrower than that anywhere) + a "Cancel this draft" button on the detail screen, all tested (unit + live-DB + E2E). **Still needs Alpesh's own action:** Claude Code cannot write to the protected contract folder (PEN-017) - the domain entities contract's own `receiving_sheet.status` `check_constraint` (around line 464) still reads `"status IN ('DRAFT', 'PENDING_PACKING', 'PENDING_WAREHOUSE', 'LOCKED')"` and needs one word added: `"status IN ('DRAFT', 'PENDING_PACKING', 'PENDING_WAREHOUSE', 'LOCKED', 'CANCELLED')"`. Paste that one-line replacement in yourself (same process as PEN-014's own patch) whenever convenient - the running application does not depend on it, only the contract text's own accuracy does. |
| PEN-034 | New pallets created at Receiving Sheet lock default to `pallet_type = "PLASTIC"` - not specified anywhere in the locked contracts | **PARTIALLY ANSWERED (Loop 39)** - Alpesh confirmed WOODEN pallets are real and will be used; the follow-up question of where/when pallet type is chosen is still open | Every pallet is still created as PLASTIC - WOODEN cannot be selected yet | Alpesh's Loop 39 answer ("Yes, Real use me aayenge...") confirms this is real future scope, not a hypothetical - but did not say where the pallet-type choice should happen: on the Receiving Sheet itself (one type per sheet, like `default_pallet_status` already works), per pallet row (like carton condition), or derived from the material/plant somehow. None of those is a smaller guess than the others, so `src/lib/receiving-sheet-lock.ts` still hard-codes PLASTIC rather than picking a UI shape unprompted. Needs one more decision from Alpesh: where does pallet type get chosen? |
| PEN-035 | New pallets created at Receiving Sheet lock resolve their warehouse from the material's own `plant_origin`, not from any field on the receiving sheet itself | **RESOLVED (Loop 39)** - Alpesh confirmed Sabarkantha is real (material transfer only, no location-wise stock tracking needed there) | Locking a receiving sheet for a SABARKANTHA-plant material now works | Alpesh's Loop 39 answer: "Yes, Real use me aayenge lekin vaha sirf material transfer hoga, vaha location wise stock dikhane ki jarurat nahi hai." `drizzle/seed/warehouses.ts` now seeds a second real warehouse row, `SABARKANTHA-FG` (sapCode `SKFGA` - "SK FG Approved Warehouse - Frozen", the real Appendix D code playing the same role `LMFGA` plays for Limbasi), with `locationStructure: "FLAT"` and deliberately no location grid seeded for it - not an oversight, the actual decision (unlike PEN-008's real 1442-row Limbasi grid, sourced from the DSR file's CR sheets - no equivalent exists or is needed for Sabarkantha). `src/lib/receiving-sheet-lock.ts` needed no code change - it already resolves a pallet's warehouse from the material's own `plant_origin`, and now finds a real row for SABARKANTHA instead of throwing. Putaway/Rack Map remain Limbasi-grid-only, correctly - Sabarkantha pallets get a warehouse (for transfer tracking) but never a location, matching the decision. |
| PEN-032 | The package self-integrity guard AND the static guard (both in CI's first job, `harness-preflight`) have been failing for many loops already, for reasons unrelated to any single loop's own work | **Package-integrity guard MOSTLY RESOLVED (Loop 40)** - Alpesh applied the exact 4-line deletion himself, verified working; static-guard's 3 findings are separately PEN-022, already accepted, unchanged | Blocks CI's `harness-preflight` job the moment this branch is pushed to or PR'd against main/develop (it gates every later CI job via `needs:` - so CI has likely never actually run green on this branch); does not block local dev/test today | Discovered while checking this guard's real exit code before evaluating Alpesh's contract patch (a prior loop's own check of this guard script mis-measured its exit status through a shell pipeline - piping its output into `tail` and then reading `$?` captures `tail`'s exit code, not the guard's own - so several earlier loop checkpoints' "harness checks: PASS" claims for this specific guard were not actually verified correctly). Running the guard directly and reading its real exit code shows 1 (failing), with 4 pre-existing mismatches: `.harness/loop-state.json` (changes almost every turn by the loop-stop-gate hook's own design - it is never expected to stay pinned), `README.md`, `docs/PENDING_ITEMS.md`, `docs/PROGRESS.md` (all edited legitimately across many loops' own documentation discipline). The guard compares each tracked file's live SHA-256 against a hash pinned in a checked-in upgrade-file manifest at some earlier baseline snapshot, and nothing in this repository documents a procedure for refreshing that manifest as normal engineering work happens - so once loop-state.json changed even once (which happens by design, every single turn), this check was mathematically guaranteed to start failing, likely from very early in this project's history, not from anything recent. The domain entities contract file itself (the one Loop 31's review concerns) is NOT among the current mismatches - it still matches its pinned hash, confirming it has genuinely not been touched by Claude Code, consistent with PEN-017. Applying Alpesh's contract patch will add a 5th mismatch line to an already-failing check, not newly break a passing one. Needs Alpesh's decision: either the manifest gets refreshed through whatever "documented approval process" the project intends (none is written down yet - itself a gap), or the manifest's own design intent needs revisiting (e.g., excluding files that are expected to change every loop, like loop-state.json and the two running logs). Not fixed or routed around here - regenerating this manifest is itself a protected-path write (the manifest file is explicitly named in the safety hook's own protected-path list), so it has exactly the same PEN-017 constraint as the contract folder itself. **Loop 39: Alpesh chose "exclude the always-changing files."** Claude Code still cannot write this file - the package-integrity checker script has no exclusion mechanism at all (it checks every line in the upgrade-file manifest unconditionally), so the fix is deleting 4 specific lines from that manifest, not a script change. **Exact lines to delete** (by their path column, wherever they fall as line numbers shift): the lines ending in the harness loop-state file, README.md, and this document plus the progress tracker document. Nothing else in the manifest should change - every other pinned hash still correctly protects a file that is NOT supposed to change without the same review this project already applies to contract patches. After deleting those 4 lines, re-run the package-integrity checker once yourself to confirm it prints a clean pass before pushing. **Loop 40 verification:** Alpesh applied both his own contract patch (adding CANCELLED, per PEN-033) and this exact manifest deletion directly to `main` (commits `02e9567`/`13db29d`), merged cleanly into the working branch. Ran the checker directly (not through a shell pipeline this time, to avoid the exact `tail`-swallows-the-real-exit-code bug this item's own earlier finding already flagged): the 4 always-changing files are gone from the mismatch list, exactly as intended. One mismatch remains - the domain entities contract file itself - and this is correct, expected behavior, not a new problem: the manifest's own pinned hash for that file was never updated to match Alpesh's just-applied, human-reviewed CANCELLED edit (or, before that, Loop 34's original 10-entity patch - this file has likely shown some mismatch since then, this item's own original finding already said applying that patch would "add a 5th mismatch line"). The guard is doing exactly its job: flagging that this specific protected file's content differs from the last pinned baseline, which is true and was a real, authorized, human-made edit - not a false positive to silence. If Alpesh wants a fully clean pass after a future contract edit, the same person applying that edit would need to also compute and paste in its new SHA-256 in the same commit; that is optional polish on top of what Loop 39/40 already fixed (removing the 4 files that were never supposed to be pinned at all), not a remaining defect. **Loop 49 addition:** the CI workflow file itself now also mismatches, for the identical already-understood reason - TASK-014's own real, human-reviewed fix to it (see PEN-048) was never followed by a baseline-hash refresh, the same one-line-per-file gap this item's own Loop 39/40 resolution already lives with for the domain entities contract. Two mismatches now, both real and both already understood, not a new class of problem. |
| PEN-031 | The real DSR "IN-OUT" sheet is a composite multi-section print layout, not a simple per-transaction daily summary - "In-Out Summary" (TASK-011, D-10) cannot be built as literally "matching DSR IN-OUT sheet" | **RESOLVED (Loop 39)** - Alpesh approved the simple-aggregation reading: "Abhi ke liye simple aggregation, hum baad me sochenge" | TASK-011's In-Out Summary bullet is now built | Built exactly the disclosed alternative this item proposed: `src/lib/business-rules/in-out-summary.ts` classifies each `stock_ledger.transaction_type` as IN (INWARD/TRANSFER_IN/BULK_RECEIVE), OUT (DISPATCH/TRANSFER_OUT/BULK_SEND), or neither (HOLD/RELEASE/MOVE/ADJUSTMENT are status-only, not a physical quantity change - deliberately excluded from both buckets rather than guessed into one), and aggregates by (date, shift, material). `GET /api/reports/in-out-summary` (gated `stock.view_summary`, same as the Stock Ledger list) serves the aggregate; a new `/stock/in-out-summary` screen shows it. Alpesh's second instruction - "aisi jitni bhi details hai ye download me chahiye" - is why the export (`GET /api/reports/in-out-summary/export`, gated `stock.export`) is two Excel sheets, not one: "Summary" (the aggregate) and "Detail" (every real stock_ledger row that fed it, with its own transaction type and direction) - so every number on the Summary sheet is traceable back to real transactions in the same file, not a number to trust blind. Today's real data only ever produces INWARD rows (Dispatch/Transfer/Bulk are TASK-007/008/009, not built yet), so the IN column is the only one with real numbers right now - disclosed on the screen itself, not hidden. |
| PEN-030 | Real Clerk cannot be verified end-to-end through a browser (E2E/Playwright/screenshots) from inside this specific sandboxed session | **DIAGNOSED, ENVIRONMENT-LIMITED - not a Clerk configuration defect** (Loop 29) | Blocks E2E/browser proof of any Clerk-gated login flow in this sandbox only | Root-caused, not just worked around: `giving-ocelot-6009.clerk.accounts.dev`-style dev-instance keys need the browser to complete a one-time "dev browser" handshake redirect to Clerk's own `.accounts.dev` domain (signaled by the `x-clerk-auth-reason: dev-browser-missing` header above) before any page can render past Clerk's middleware. Checked this sandbox's own egress proxy status endpoint directly (`curl -sS "$HTTPS_PROXY/__agentproxy/status"`) and confirmed it returns a policy 403 for that Clerk domain - and, as a control, for an unrelated arbitrary external domain (`www.google.com`) too, proving this is a general organization-level egress allowlist gap for this sandbox, not something specific to or caused by this project's Clerk setup. Confirmed empirically both ways: temporarily renamed `.env.local` aside (forcing stub mode) and reran `tests/e2e/app-shell.spec.ts` - 9/9 passed instantly with no tunnel error, then restored the real keys and the same suite hit the tunnel error on every route (because `clerkMiddleware()`'s matcher covers nearly all pages, so every page load now attempts the blocked redirect). Per this sandbox's own documented troubleshooting guidance (`/root/.ccr/README.md`: "Do not retry or route around it - report the blocked host"), this was reported, not retried or bypassed. Not fixable from inside this session - the Clerk configuration itself (keys, middleware, provider, sign-in/sign-up routes) is correct and is expected to work normally in any environment with unrestricted internet access (a real deployment, or a developer's own machine); this is purely a this-sandbox network-policy gap. Needs either: this session's egress allowlist widened to include `*.accounts.dev` (an infrastructure decision outside this loop's scope), or E2E/browser verification of the real-Clerk login flow done outside this sandbox. Until then, this repository's Clerk-gated routes remain proven only at the two levels Loop 29 could actually exercise from here: the HTTP-header-level check above (real keys are active), and the existing stub-mode honest-401/403/503-refusal E2E coverage (PEN-021) - not a real logged-in browser session. |
| PEN-011 | Offline/PWA scope conflict | **DECIDED, DELIBERATELY DEFERRED (Loop 39)** - Alpesh wants offline/PWA support, but chose to finish the other core modules (Dispatch, Bulk, Transfers, Maintenance) first | Affects every screen already built and every one still to come - not scheduled yet | Alpesh's Loop 39 answers: "Offline/PWA support chahiye" (wanted), then, when asked how to sequence it, "Baaki features pehle complete karo" (finish the other features first). This is a real architectural decision, not a bounded CRUD task - it needs a service worker, an offline mutation queue, and (the hard part) a conflict-resolution rule for what happens when two people edit the same record while offline and both come back online (last-write-wins? merge? block the second one?). None of that is written down anywhere yet. Deliberately not started - correctly sequenced after the remaining core modules by Alpesh's own choice, not a blocker being avoided. Revisit once TASK-006/007/008/009/010 are done: needs a dedicated scoping/design pass (including the conflict-resolution answer specifically) before implementation begins. |
| PEN-019 | `npm run lint` has never actually completed a run since it was added in Loop 2 | RECORDED, non-blocking | None found yet — surfaced, not fixed | Discovered in Loop 19 (the first loop to actually invoke this script as part of a full-regression pass): `next lint` requires answering a one-time interactive ESLint setup prompt ("Strict" / "Base" / "Cancel") that this non-interactive environment cannot answer, so the command exits 1 without linting anything. Not a regression from any change in this window. Not resolved: picking an ESLint preset is a tooling decision outside a regression-checkpoint loop's scope, so it is recorded per the STOP RULE rather than guessed. Needs either a human-run interactive `next lint` once (to write the config), or an explicit instruction on which preset to pre-seed non-interactively. |
| PEN-020 | No production Cloudflare D1 binding wiring exists for the Next.js app | RECORDED, non-blocking today | Blocks any real deployment; does not block local dev/test | Loop 21's `src/lib/db.ts` (first real database access code in the app) connects local API routes to the real local D1 (Miniflare) SQLite file directly with better-sqlite3, because there is no `@cloudflare/next-on-pages` (or equivalent) adapter in this repository giving a genuine `env.DB` binding inside a Next.js request. That adapter choice is an infrastructure decision outside a single bounded CRUD loop's scope, so nothing was invented or assumed here - this local-only connection module must not be treated as proof production access works, and gate #10 (D1 stays local-only until a remote connection is verified) still applies in full. |
| PEN-021 | Clerk stub mode makes every permission-gated mutation route fail, and (until this loop) failed with an opaque crash rather than a clean status code | **Root cause fixed this loop (Loop 21); the underlying constraint remains** | Every future mutation-gated task (Receiving Sheet, Holds, Dispatch, ...) until PEN-010 closes | Found by actually running the first real mutating API route against a live dev server, not just a mocked unit test: `requireRole`/`requirePermission` (Loop 16) call Clerk's real `auth()`, which throws its own internal error when `clerkMiddleware()` never ran - true today because Clerk has no real keys. Fixed at the smallest safe layer: `src/lib/auth.ts` now checks the same config-status signal the middleware already uses before calling `auth()`, and raises a new, specific `AuthNotConfiguredError` (503) instead. The security decision is unchanged - no mutation can be authorized without a real session - only the failure mode became predictable and testable (3 new regression tests in `tests/unit/auth.test.ts`). The remaining, un-closeable-from-here constraint: no mutation-gated business workflow can be exercised end-to-end through a real logged-in browser session until a real Clerk application exists: today's E2E coverage for such routes can only prove the read path and the honest-refusal path, per the Loop 21 evidence in the progress document. |
| PEN-022 | static-guard flags read-only routes with no create/edit handler in the same file as lacking an auth guard | RECORDED, deliberate decision - not a false positive worked around, and not silently ignored | None - these routes ship as public reads within this app | These GET-only route files (`src/app/api/masters/statuses/route.ts`, `src/app/api/masters/sap-codes/route.ts`, and `src/app/api/pallets/route.ts` added in Loop 24) never call `requireRole`/`requirePermission`/Clerk, so static-guard's whole-file heuristic correctly reports "no auth call anywhere in this file" - unlike Material/Warehouse Master/Location's GET, which each share a file with a POST that does call the permission gate and so are not flagged, even though those GETs are equally auth-free today. Decision made, not guessed silently: this window's own gate-availability finding (PEN-021) means requiring a real session on any of these routes would make them unreadable by anyone at all until a real Clerk application exists, which is a usability regression with no corresponding rule in the locked entities/invariants/workflow contracts that calls for gating reads - the pallets route is a little more operationally sensitive than the two fixed reference-data lists, but it is still read-only, and pallet creation/mutation itself (the actually sensitive half) stays fully gated. Nothing was added to any of these files to dodge the regex; the guard's per-file finding is left visible and true. Needs a human decision only if plain reads should ever require a session - not before. **Loop 36 addition:** applied this same precedent deliberately, not by omission, to the new Receiving Sheet GET routes (`src/app/api/receiving-sheets/route.ts`, `.../[id]/route.ts`) - both originally shipped gated behind `requireRole`, then un-gated once building the real UI screen made the same PEN-021 usability problem concrete (nobody, in any role, could view a receiving sheet they had not personally just created). Neither is flagged by this guard anyway, since both files also contain a `requirePermission` call for their own POST/PATCH handler - the guard's own whole-file heuristic, not a gap being routed around. **Loop 31 correction:** this guard script exits with a real nonzero status whenever it reports any finding at all, these 3 deliberately-accepted ones included - it has no severity/accepted-vs-new distinction built in. Several earlier loops' own checks of this script were run through a shell pipeline whose captured exit code actually reflected a downstream `tail` command, not the guard itself, so "harness checks: PASS" claims covering this specific guard in those checkpoints were not accurate for it - see PEN-032 for the same mismeasurement pattern found in the package self-integrity guard. This guard has therefore likely been returning a failing exit code since Loop 24, for the same 3 already-understood, already-accepted reasons documented above - not a new regression, just newly measured correctly. **Loop 41 addition:** the same precedent applies deliberately, not by omission, to the new `src/app/api/loading-sheets/[id]/route.ts` (TASK-008) - the real permission matrix gives R01/R03/R09 `loading_sheet.create`/`.verify` and R04/R10 `loading_sheet.view`, but no role combination leaves this plain detail read ungated without making the screen unusable to its own creators, the identical PEN-021 usability problem already found for Receiving Sheet and Hold Management reads. `src/app/api/loading-sheets/route.ts` (the list) is not flagged, since it shares a file with the gated POST create handler - same as every other list route in this project. Now 4 pre-existing, already-accepted findings, not 3 - still zero new ones from anything actually being routed around. **Loop 42 addition:** the same precedent applies again to the new `src/app/api/bulk-pallets/route.ts` (TASK-007) - R01 (the warehouse-side actor for the BULK -> QC_HOLD transition itself, per pallet-status.ts) has no `bulk.view`-style grant anywhere in the real permission matrix (only R06/R08 do), the identical usability problem. 5 pre-existing, already-accepted findings now. **Loop 43 addition:** the same precedent again for `src/app/api/transfer-orders/[id]/route.ts` (TASK-009) - no role has an explicit "transfers.view" grant anywhere in the real permission matrix at all (only `.create`/`.dispatch` exist), the identical usability problem. `src/app/api/transfer-orders/route.ts` (the list) is not flagged, same reason as every other list route in this project - it shares a file with the gated POST create handler. 6 pre-existing, already-accepted findings now. **Loop 44 addition:** the same precedent again for `src/app/api/maintenance-tickets/[id]/route.ts` (TASK-010) - R01/R02 (who create tickets, close, and verify fixes) have no explicit "maintenance.view" grant (only R03/R11 do), the identical usability problem. 7 pre-existing, already-accepted findings now. **Loop 47 addition:** the same precedent again for the new `src/app/api/dashboard/route.ts` (TASK-012) - no role has an explicit "view dashboard" grant anywhere in the real permission matrix at all, the identical usability problem; unlike the other 7, this file's own per-panel `hasPermission()` checks (see PEN-046) mean the guard's whole-file heuristic is doubly a false negative here - real permission checks do exist in the file, just not gating the whole handler. 8 pre-existing, already-accepted findings now. |

| PEN-023 | TASK-005 (Putaway + Rack Map)'s own "Location validation" bullets do not map to any locked invariant or schema field | **CLARIFIED (Loop 24) - putaway/move UNBLOCKED for the part the canonical source actually specifies; the exact "pallet type" mechanism is still open** | Was blocking TASK-005's putaway/move business logic; no longer fully blocking - see resolution | The implementation spec's TASK-005 bullets turned out to trace back to the canonical flow document's own Flow 2 ("PUTAWAY & LOCATION MANAGEMENT"), which this loop read directly rather than staying with the architecture blueprint's silence on it - the flow document is the higher-authority source and it does describe "System validates: Location is empty / Location accepts that pallet type / Pallet weight within location capacity" with the exact example error message "Location X is occupied by Pallet Y (MATERIAL_CODE)". That error message and the accompanying Rules section ("Each floor position holds one pallet... Multiple pallets of same material can be in same block... this is normal and expected") gave a real, non-invented basis for: location must not be BLOCKED; an EMPTY location may be assigned; an OCCUPIED/PARTIAL location may only accept a pallet of the SAME material, and only if capacity allows one more. `src/lib/business-rules/location-guard.ts` implements exactly that (7 unit tests + a live-database integration test), and `/api/storage/putaway` + `/api/storage/move` use it. What is still deliberately NOT enforced, because no field or rule defines its actual mechanism anywhere (flow document, architecture blueprint, entities contract): "Location accepts that pallet type" (no pallet-type-per-location field exists) and a hard location-level weight cap (the flow document's Step 1 reads as a placement *suggestion* heuristic - "heavier pallets on lower floors" - not a validation block, and `capacity_pallets` is a pallet count, not a weight). Also explicitly not supported yet: `capacity_pallets` > 1 (true multi-pallet-per-location) - `locations.current_pallet_id` is a single column, and the guard refuses that case outright rather than silently mishandling it. |
| PEN-024 | Putaway/move cannot write a stock_ledger entry, so "Location history per pallet must be traceable" (Flow 2's own Rules section) is not yet met | **RESOLVED (Loop 37)** - both routes now write real, append-only stock_ledger MOVE rows | Location move history/audit trail via putaway/move is now queryable, same as Receiving Sheet's own INWARD entries | `stock_ledger.batch_id` is `NOT NULL`, but the schema originally had no Pallet-Batch relationship table at all (ENTITY-004) - see the full original finding below, unchanged as history. **Loop 35 update:** PEN-014/017 closed (Loop 34), and Loop 35 built the real `pallet_batches` table this was waiting on. **Loop 37 update (closes this item):** `/api/storage/putaway` and `/api/storage/move` now each write one append-only `stock_ledger` row (transaction_type `MOVE`) per `pallet_batches` entry the pallet carries, inside the same transaction as the location/pallet update - qty/weight are unchanged (0) on a plain move, since only the location itself is new; the move route's mandatory reason lands in the ledger row's own `remarks` column. A pallet with zero `pallet_batches` rows (a pre-Loop-35 fixture, or anything inserted directly rather than through the Receiving Sheet flow) still has no real batch_id to write with, so that case is silently skipped, same reasoning as the original finding - narrower now, not eliminated: every pallet created through the real Receiving Sheet flow always has at least one `pallet_batches` row, so this residual gap only affects hand-inserted test/legacy data, not real production pallets. Proven against real local D1 writes (not just mocked) by the new "Putaway/move write real stock_ledger rows" block in `tests/unit/mutations-live.test.ts`. |
| PEN-025 | Rack Map (SCREEN-003) cannot show the "Yellow = mix (different batches - allowed for same material)" legend color | **RESOLVED (Loop 37)** | Cosmetic only - the other 5 legend colors were already correct | Same root cause as PEN-024, now closed the same way. **Loop 37 update (closes this item):** `src/lib/rack-map.ts`'s `rackMapCellColor` now takes an optional `distinctBatchCount` on the occupant and returns `yellow` when it is greater than 1 (undefined/1 still reads as a single batch, never guessed as a mix); `GET /api/pallets` now joins a grouped `count(distinct batch_id)` over `pallet_batches` and returns it per pallet; the Rack Map page passes it through to the color function and to the pallet-detail popup ("Mix of N batches"). Precedence, not specified anywhere in the flow document, chosen deliberately: BLOCKED/EMPTY first (location-level facts), then HOLD (an operator needs "do not touch" before contents detail), then the batch-mix question, then plain partial/full - see the module comment in `rack-map.ts` for the full reasoning. Covered by new unit tests in `tests/unit/rack-map.test.ts`. |
| PEN-036 | The canonical flow document's Flow 3 Step 1 says a hold can be placed on "OK/RELEASED" material ("Status changes: OK/RELEASED → HOLD"), but the locked workflow contract's `pallet_status` state machine defines no `OK -> HOLD` transition at all - only `QC_HOLD -> HOLD` | RECORDED (Loop 38), not blocking - the real, common case (QC inspecting a freshly received pallet) is fully built | Placing a hold on a pallet that was already released to OK (e.g. a later-discovered defect) cannot be done through Hold Management today | Per the AUTHORITY ORDER (the flow document outranks the machine-readable contracts), the flow document's prose implies this transition should exist - but `src/lib/workflows/pallet-status.ts` (Loop 17, reviewed then as "already fully contracted") transcribes the workflow contract's `TRANSITIONS` array field-for-field, and that array has no `OK -> HOLD` entry, no actor list for it, and no stock_ledger transaction-type mapping for it. Inventing one here - a real authorization rule and a real ledger-writing transition - would mean guessing an actor list and ledger semantics no locked source actually states, which the STOP RULE says not to do. `POST /api/holds` therefore only accepts pallets currently `QC_HOLD` (matching workflows.yaml exactly), which is also the realistic, dominant case in this domain (every pallet starts `QC_HOLD` at Receiving Sheet lock and is inspected before its first release) - not a narrow edge case being dodged. Needs Alpesh's decision: either workflows.yaml gets an `OK -> HOLD` transition added (with an actor list and ledger semantics), or the flow document's "OK/RELEASED" wording is acknowledged as describing an edge case out of this project's v1 scope. |
| PEN-037 | Flow 3 Step 4 describes releasing "full batch, specific pallets, partial quantity" - Hold Management (TASK-006) only supported releasing/rejecting an entire hold record (all its pallets) at once | **RESOLVED for the "specific pallets" granularity (Loop 50)** - Alpesh: "Hold release Partial bhi kar lo...1200 boxes hold ho usme se 300 ya 400 Release karna pade" | Can now release/reject a real, chosen subset of a multi-pallet hold's own pallets, independently, more than once | Built exactly the schema addition this item's own original finding said was needed: `hold_pallets` gained its own `status`/`released_by_id`/`released_at`/`release_remarks` columns (migration 0012), independent of the parent `hold_record`'s own status - `POST /api/holds/[id]/release` and `.../reject` now take an optional `palletIds` array (omit it to act on every still-ACTIVE pallet, the exact same whole-hold behavior this route already had - no existing caller needed to change, all pre-Loop-50 tests pass unmodified). `hold_records.status` is no longer set directly by these routes - it is a pure rollup of its own hold_pallets rows (`rollupHoldStatus` in `src/lib/business-rules/hold.ts`): ACTIVE while every pallet is still ACTIVE, the same RELEASED/REJECTED as before once every pallet shares that one terminal status, and a new real `PARTIALLY_RELEASED` value (added to the CHECK constraint) whenever the set is a genuine mix - including a hold whose every pallet is DONE but split between RELEASED and REJECTED, which correctly stays PARTIALLY_RELEASED forever rather than being force-fit into one of the other two. **Deliberately still NOT built, disclosed not guessed:** true sub-pallet quantity splitting (dividing ONE pallet's own cartons between two different statuses) - no locked schema or contract anywhere supports splitting a single pallet's real `total_cartons`/`total_weight_kg` between two states, and inventing that mechanism would be exactly the guess the STOP RULE exists to prevent; where a hold's own pallets don't already divide into the quantity a user wants released, the honest answer today is placing the hold at a finer pallet-level grain up front (Flow 3 Step 3 already lets QC choose which specific pallets to hold), not a fabricated within-pallet split. A real, unrelated drizzle-kit generated-migration bug was found and hand-fixed while building this (the same class PEN-039/040 already documented): the generated table-recreate tried to `DROP TABLE hold_records` while `hold_pallets` still had a live foreign key pointing at it, which fails on this project's own real local D1 with `FOREIGN KEY constraint failed` - `PRAGMA foreign_keys=OFF` does not help here because SQLite treats it as a no-op inside an already-open multi-statement transaction (this whole migration file runs as one, via D1's own `batch()` primitive per PEN-044), a real interaction this project had not hit before now (every earlier CHECK-constraint recreate happened to drop a table nothing else referenced). Fixed by hand-reordering the migration so the CHILD table (`hold_pallets`) is always fully dropped and later rebuilt around the PARENT's own recreate, never the reverse - see migration `0012_green_miss_america.sql`'s own header comment for the full reasoning, and its own backfill logic for how every pre-existing `hold_pallets` row's new per-pallet status was derived honestly from its own hold_record's already-known status, not guessed as a blanket default. |
| PEN-038 | This project has no real notification channel anywhere (no email/SMS/push integration exists at all) - Hold Management's "Daily digest notification to QC" and "Production notification on hold" bullets (TASK-006 IN SCOPE) are not built | RECORDED (Loop 38), disclosed scope reduction, not silently dropped | The QC follow-up loop is real and queryable (see resolution) but nothing is actually pushed to anyone outside this app | Neither bullet has ever had an infrastructure decision behind it (no notification provider is named anywhere in the locked contracts, and no other feature in this project - not Receiving Sheet, not putaway/move - has ever sent a real notification either). Building a fake "notification sent" confirmation with nothing behind it would be worse than not building it. What IS built and real: the "Follow-up nudge" action (`POST /api/holds/[id]/followup`, `holds.followup_nudge` - R03) increments the hold record's own `qc_followup_count` and sets `last_followup_at` (both already part of the entity's own contracted attribute list) - a permanent, queryable record of "QC was nudged about this hold, N times, most recently at T", visible on the dashboard, not a simulated notification. Needs Alpesh's decision on a real channel (email via a provider, SMS, or an in-app inbox) before the digest/escalation bullets can be built for real. |
| PEN-026 | Once any code writes a stock_ledger row, the material/batch/pallet/warehouse/user rows it references become permanently undeletable | **VERIFIED, real and correct behavior** (Loop 26) | Testing/local-dev housekeeping only - not a defect | Confirmed directly, not assumed: this local D1 (better-sqlite3) connection has real SQLite foreign-key enforcement ON by default (`PRAGMA foreign_keys` = 1, unprompted), and stock_ledger's own append-only triggers (INV-009) already forbid deleting the ledger row itself. Together, once a ledger entry references a row, that row can never be deleted while foreign keys stay enforced - a correct property for an audit-grade ledger, not a bug. `tests/unit/stock-ledger-read.test.ts` had to switch from this repository's usual delete-then-recreate test fixture pattern to a find-or-create (idempotent) one because of it, and gives its fixture pallet a real location so it does not permanently pollute the Storage/Putaway screen's "awaiting putaway" list either - `tests/e2e/storage-putaway.spec.ts` was also hardened to check its own fixture pallet inside the "Awaiting putaway" section rather than assert an exact global count, since the local D1 file is genuinely shared, accumulating state across every test suite that has ever written a permanent ledger-referenced row. Any future test (or seed script) that writes to stock_ledger needs the same idempotent-fixture treatment, not the delete-first pattern used everywhere else. |
| PEN-039 | TASK-008 (Loading Sheet, Flow 5/6): the `loading_sheet` entity (ENTITY-013) has no pallet/material/batch/quantity fields at all, and no `dispatch_order` entity exists anywhere in the contract despite being named as its own IN SCOPE bullet | RECORDED (Loop 41), not blocking - built using the same disclosed-translation precedent already applied elsewhere, not invented from nothing | TASK-008's full Flow 5/6 lifecycle (pick -> stage/load -> verify -> gate-pass -> dispatch) is built and tested; only the contract text's own completeness is the open question | Four real gaps found while building this task, each resolved the same way this project already resolves this class of gap - by building a disclosed, non-invented translation, not by guessing new business rules: **(1)** ENTITY-013's own attribute list has "Material Details (table)" and "Pallet-wise loading sequence" only in the flow document's Step 3 prose, no real field - built `loading_sheet_pallets` as a real child/junction table (`drizzle/schema.ts`), the same translation already applied to `pallet_batches`, `receiving_sheet_pallets`, and `hold_pallets` (PEN-014's own precedent), one row per pallet picked, carrying `fifo_override_reason` for INV-010's "logged override with reason". **(2)** No `dispatch_order` entity exists to create against, even though the implementation spec names "dispatch order creation" as its own bullet - read the loading sheet's own DRAFT status as the practical dispatch-order input step (its header fields ARE Flow 5 Steps 1-3's input), rather than inventing a whole separate uncontracted entity with its own table/status/permissions; pick-list generation (`dispatch_orders.generate_pick_list` exists in the real permission matrix but names no entity to generate from) is implemented as a client-side computed helper (FIFO-sorted `<select>` in the loading-sheet detail page), not a new server endpoint or stored artifact. **(3)** `workflows.yaml` has no explicit `loading_sheet_status` transition table the way it has one for `pallet_status`/`receiving_sheet_status` - `src/lib/business-rules/loading-sheet.ts`'s `nextLoadingSheetStatus` derives the 6-state linear sequence (DRAFT -> STAGING -> LOADED -> VERIFIED -> GATE_PASSED -> DISPATCHED) directly from Flow 5's own Steps 1-6 narrative, a disclosed non-branching reading, not a guessed one - no alternate paths or skips are invented. **(4)** The pick route (`POST /api/loading-sheets/[id]/pallets`) refuses any pallet carrying 0 or more than 1 batch - only a single-batch pallet can be picked onto a loading sheet, because `loading_sheet_pallets` (like `hold_pallets` before it) has exactly one `batch_id` column per row and no contracted source says how to split a mixed-batch pallet's dispatch across two lines. **Two permission choices, made from the real matrix, not invented:** gate pass recording (VERIFIED -> GATE_PASSED, Flow 5 Step 5 - security records vehicle exit) uses `gate_pass.record_exit` (R10), not `gate_pass.generate` (R03) - the matrix has both, and "record exit" is the literal action this step performs; dispatch itself (GATE_PASSED -> DISPATCHED) has no `loading_sheet.dispatch` permission string anywhere in the matrix, so it reuses `pallet_status.ts`'s own already-contracted `OK -> DISPATCHED` actor list (R03/R09/R10, Loop 17) via `requireRole` directly, instead of inventing a new permission string the matrix doesn't define. Needs Alpesh's decision only if any of these four readings should be different - none is currently blocking, since each is the dominant/only reading the locked sources actually support. |
| PEN-040 | TASK-007 (Bulk Management, Flow 3 Step 7 / GS-004): there is no dedicated "bulk record" entity anywhere in the domain entities contract - the task's own MUST READ note names only "BULK status" (the pallet_status value already in pallets' CHECK constraint) | RECORDED (Loop 42), not blocking - built using the same disclosed-translation precedent as PEN-039, not invented from nothing | TASK-007's full bulk dashboard/repack lifecycle is built and tested; only the contract text's own completeness is the open question | Three real gaps found, each resolved by reusing an already-contracted, already-built piece rather than inventing new schema or a new state machine: **(1)** "Bulk material: where it is, how old, how much, **reason** (over-production vs defective)" (Flow 3 Step 7's own tracked-fields list) has no field anywhere - `receiving_sheets` gained a nullable `bulk_reason` column (CHECK-constrained to exactly the two values already contracted as bulk-specific entries in ENTITY-011's own `fixed_hold_reasons` list - "Over-production (bulk)"/"Defective fries (bulk)", reused verbatim as `BULK_REASONS` in `src/lib/business-rules/bulk.ts`, not a new invented vocabulary), required exactly when `default_pallet_status='BULK'` and forbidden otherwise (`assertBulkReasonConsistency`). **(2)** "Bulk -> Packing transfer record" and "Repack receipt: new receiving sheet linked to original bulk" (two separate IN SCOPE bullets) are combined into one atomic action (`POST /api/bulk-pallets/[id]/repack`), not two - the workflow contract's own `pallet_status` state machine has exactly one transition for BULK at all (`BULK -> QC_HOLD, action: bulk_repacked, actors: [R01, R06]`, already fully contracted since Loop 17, `ledgerTransactionType: BULK_RECEIVE`), with no intermediate "at packing, not yet repacked" pallet status to represent a real two-step user flow the way the reference mockup's own "Send to Packing" button visually implies - inventing one would mean adding a pallet_status value the locked CHECK constraint doesn't have. The repack route therefore fires this one real transition (writing a real `BULK_RECEIVE` ledger row, `referenceType MANUAL_MOVE`/`referenceId` = the pallet itself, same convention already used by putaway/move for this reference type) at the same moment it creates the new, empty, linked DRAFT receiving sheet (`receiving_sheets.original_bulk_pallet_id`, a second new nullable additive column, for GS-004's own "traceability" acceptance criterion) - the new sheet is then completed through the existing, already-built, already-tested Receiving Sheet flow ("goes through FG Receiving Sheet flow again", per the flow document), not reimplemented. **(3)** No numeric bulk-aging threshold is stated anywhere (unlike Hold Management, whose own SCREEN-004 mockup gave ">3d amber / >7d red" directly) - the Bulk dashboard reuses those exact same Hold Management thresholds for visual consistency, a disclosed UI-carryover choice (`bulkAgeBucket` in `bulk.ts`), not a new invented business rule. **Also disclosed, not silently dropped:** "Bulk aging alerts" (the 5th IN SCOPE bullet) has the same real gap as PEN-038 (Hold's own daily-digest bullet) - no notification channel (email/SMS/push) exists anywhere in this project; the aging itself is real and visible (color-coded on the dashboard), nothing is pushed to anyone. A genuine drizzle-kit code-generation bug was also found and fixed while building this: its own generated table-recreate migration for adding these two new nullable columns (`0009_cold_the_twelve.sql`) wrote an `INSERT ... SELECT` that selected the two brand-new column names FROM the OLD table (which never had them), which is invalid SQL - it happened not to be caught by applying it incrementally to the already-existing local D1 file (that copy of `receiving_sheets` had 0 rows to copy at the time, though the statement should still fail at SQL-compile time regardless of row count - not fully explained, flagged here rather than assumed harmless) but broke immediately when 3 existing test files (`schema-constraints`/`seed-data`/`location-grid`.test.ts) rebuilt a fresh database by replaying all migration files from scratch, exactly as they are designed to. Fixed directly in the migration file (selecting `NULL` literals for the two new columns instead of the non-existent source columns - the standard, correct SQLite pattern for backfilling a new column during a table-recreate), re-verified via a full from-scratch replay and all 3 previously-broken test files passing again. This is a new failure mode beyond the already-documented trigger-loss risk (Loop 39) for this same class of migration - worth remembering for any future CHECK-constraint-driven table recreate that also adds new columns in the same change. |
| PEN-041 | TASK-009 (Inter-Warehouse Transfers, Flow 4): the locked workflow contract's `pallet_status` state machine defines `OK <-> IN_TRANSIT` only - there is no `HOLD -> IN_TRANSIT`, `BULK -> IN_TRANSIT`, `IN_TRANSIT -> HOLD`, or `IN_TRANSIT -> BULK` transition anywhere - even though INV-017 ("Hold materials CAN transfer with hold tag", severity MEDIUM, locked) and Flow 4's own Rules section ("Bulk materials transfer with bulk tag") both require exactly that to work | RECORDED (Loop 43), not blocking - GS-003/INV-017 both pass for real, using a reading that needs no new pallet_status transition | HOLD_TAG and BULK_TAG transfers (2 of the transfer_order entity's own 3 contracted `transfer_type` values) are fully built and tested, using a different mechanism than NORMAL transfers for the pallet-status side specifically | Per the AUTHORITY ORDER, the flow document (higher authority) requires this to work, but inventing a `HOLD -> IN_TRANSIT`/`IN_TRANSIT -> HOLD` pair in `pallet_status.ts` would mean guessing an actor list and ledger semantics no locked source states, the same STOP RULE reasoning PEN-036 already applied to a related pallet_status gap. Instead, re-read what "in transit" actually needs to mean: the `transfer_order` entity's own `status` field is a **separate, fully-contracted state machine** (`DRAFT->PICKED->LOADED->IN_TRANSIT->RECEIVED->COMPLETED`, every transition explicit in workflows.yaml, unlike `loading_sheet_status`) that already tracks "this shipment is currently in transit" on its own, with no dependency on the PALLET's own status_code. So: for a **NORMAL** transfer, the pallet really does transition `OK -> IN_TRANSIT -> OK` via `pallet_status.ts`'s own already-contracted transitions (Loop 17), writing real `TRANSFER_OUT`/`TRANSFER_IN` ledger rows the established way. For **HOLD_TAG**/**BULK_TAG**, the pallet's own `status_code` is deliberately left completely unchanged throughout the whole transfer (still HOLD, still BULK) - "in transit" is represented purely by the transfer_order's own IN_TRANSIT status (joined through the new `transfer_order_pallets` junction table when a screen needs "which pallets are currently in transit"), and a real `TRANSFER_OUT`/`TRANSFER_IN` stock_ledger row is still written directly (not via `pallet_status.ts`, since no transition exists) for full INV-016 traceability - `statusBefore`/`statusAfter` both read the pallet's own unchanged status, honestly reflecting that nothing about its STATUS changed, only its location and (on receive) its warehouse. This also directly satisfies Flow 4 Step 5's own explicit receiving-side rule ("If HOLD TAG: material enters receiving warehouse with status HOLD... If BULK TAG: ... status BULK") - it falls out automatically rather than needing special-case code, since the status was never touched to begin with. **A second, smaller gap found the same way:** `CANCELLED` is a real state in `transfer_order_status`'s own `states` list, but workflows.yaml lists zero transitions in or out of it anywhere - so, per the same STOP RULE, no cancel action was built for transfer orders (`src/lib/business-rules/transfer-order.ts`'s `nextTransferOrderStatus` never produces or accepts it). **A third, disclosed scope reduction:** "3PL inward register (DSR-3PL INWARD format)" (TASK-009's own IN SCOPE bullet, GS-010's acceptance test) is satisfied by the general transfer flow itself - a NORMAL transfer received at a warehouse whose own `type='3PL'` (ENTITY-006's own real check_constraint value) is real, real-data 3PL inward, and the transfer detail/list API responses surface `destinationWarehouseType` so a screen can identify and filter it - but no dedicated report reproduces the DSR-3PL INWARD sheet's own extra columns (SAP No, SAP Executive Name, WMS In No, WMS Executive Name), since none of those has any data source anywhere in this project, the same class of gap PEN-031 already found and Alpesh already resolved with a "simple, real-data" reading for In-Out Summary - applied here the same way, not re-litigated. Needs Alpesh's decision only if any of these three readings should be different. |
| PEN-042 | static-guard's "Possible raw SQL outside ORM" heuristic (`/SELECT\s+.+FROM\s+/is`) produces a false positive on plain UI prose text, not just real SQL | RECORDED (Loop 43), verified false positive, not routed around | None - `src/app/(app)/transfers/[id]/page.tsx` has zero database access of any kind (a client component, no `@/lib/db` import) | Found while re-running the harness checks after TASK-009: the guard flagged this one file. Read it directly rather than assuming the guard was right or silencing it: the regex is dotall (`s` flag) and case-insensitive, so `.+` matches across newlines - the file's own two independent, unrelated UI strings "Select a pallet to pick." (an error message) and, over 100 lines later, "...pallet **from** the source warehouse" (a field label) are enough to satisfy `SELECT\s+.+FROM\s+` across the whole file, exactly like a real `SELECT ... FROM` statement would. This is a distinct false-positive class from PEN-022 (which is about the guard's separate "lacks obvious auth guard" heuristic) - this is its other heuristic, on a file that was never flagged for auth at all. Not fixed or routed around (the guard script is protected, PEN-017's own constraint) and not fixed by rewording the UI text either, since the current wording is the clearest, most honest copy for the screen - recorded here as a known, verified-harmless finding instead. Needs a human decision only if a tighter, less falsy version of this heuristic is wanted (e.g. requiring the matched span to be free of JSX/HTML tags, or a smaller `.{0,80}` bound instead of unbounded `.+`) - cosmetic, not blocking. **Loop 48 addition:** the same false positive fires on the new `src/app/api/stock/aging/route.ts` (TASK-013) for a related but distinct reason - this file has no UI prose at all, but does contain two separate real Drizzle `.select({...}).from(...)` calls; the same dotall unbounded regex matches from the first call's `.select(` (case-insensitively "SELECT") all the way through to the second call's `.from(` many lines later, flagging legitimate parameterized ORM syntax as "possible raw SQL". Verified the same way - zero `.prepare(`/`db.run(`/`execute(sql`/`PRAGMA` anywhere in the file, every query goes through Drizzle. |
| PEN-043 | TASK-010 (Maintenance Module, Flow 9 / GS-006): the real permission matrix names several Flow 9 actions with no exact 1:1 permission string, and ENTITY-014 has no field for two of the flow document's own real data points | RECORDED (Loop 44), not blocking - the full maintenance lifecycle is built and tested using the closest real matrix/entity reading each time, not invented ones | GS-006 (Maintenance Critical Issue) passes for real, full lifecycle including CRITICAL escalation | Four small, disclosed readings, each the closest real fit rather than a guess: **(1)** Flow 9 Step 1's own "Who can raise: any warehouse team member" has no literal "anyone" permission - `maintenance.create` (R01/R02/R03) is the real matrix's own reading of that phrase, not a narrower invented restriction. **(2)** ACKNOWLEDGED/REOPENED -> IN_PROGRESS ("start work") has no dedicated permission string in the matrix at all (only `.acknowledge`/`.resolve`/`.view`, all R11) - gated `requireRole(["R11"])` directly, reusing the one real role the matrix already assigns to every other maintenance-team action across Flow 9 Steps 3-4, the same precedent as every other "no matching permission string" gap this project has already resolved this way (Loading Sheet dispatch, Bulk repack, Transfer receive/complete). **(3)** RESOLVED -> CLOSED is named by the matrix from two different role perspectives at once - `maintenance.close` (R01) and `maintenance.verify_fix` (R02) - but ENTITY-014 has no separate `verified_by_id`/`verified_at` field to record a distinct verification step before closure, so both roles perform the one real CLOSED transition directly (`requireRole(["R01","R02"])`), not two invented sub-steps; the same two roles handle the alternate RESOLVED -> REOPENED outcome for the same reason. **(4)** Flow 9 Step 5's "REOPEN with comments" has no `reopen_comments` field anywhere in ENTITY-014's own attribute list - the comment is appended to the ticket's own `resolution_notes` (the one real free-text field the entity contracts, already used for the original resolution), prefixed `[REOPENED]`, not silently dropped or given an invented new column. **Also disclosed, not silently dropped:** Step 2's own "category-based auto-routing... notification to relevant team's dashboard" and Step 3's "if CRITICAL: SMS/urgent notification, escalation to plant head" both need a real notification channel and a "team"/"department" concept that exists nowhere else in this project - the exact same root gap PEN-038 already found and disclosed for Hold Management's own digest bullet. What is real: the `category` field itself is the routing signal (any screen can filter by it, no separate routing/assignment entity was invented), and a CRITICAL-severity open ticket shows a real, visible "escalated" notice on its own detail page - nothing is faked as sent to anyone. Needs Alpesh's decision only if any of these four readings should be different, and the same notification-channel decision PEN-038 already needs. |
| PEN-044 | Cloudflare D1 does not support multi-statement `BEGIN`/`COMMIT` transactions at all - `drizzle-orm/d1`'s own `.transaction()` sends exactly that and would crash in production | **FOUND AND FIXED (Loop 45/46)**, not blocking - confirmed empirically against the real remote D1 database, then every affected route rewritten before any production deploy was attempted | Was going to silently break nearly every mutating route (Hold, Loading Sheet, Bulk, Transfer Order, Maintenance, Storage move/putaway, Receiving Sheet lock) the moment this app ran on real D1 instead of the local better-sqlite3 stand-in | While wiring `src/lib/db.ts` to a genuine Workers `env.DB` binding (the real primitive this whole Cloudflare-over-Vercel decision was made to get), sent a literal `BEGIN` to the real remote database via the Cloudflare MCP query tool as a direct empirical test before trusting drizzle's `.transaction()` on D1 - it was rejected outright (`code 7500: "please use the state.storage.transaction()... instead of the SQL BEGIN TRANSACTION"`). Confirmed against Cloudflare's own current docs too: "D1 operates in auto-commit" - the only real atomic multi-statement primitive it exposes is `batch()` (all statements prepared upfront, no branching on an intermediate result within the group). All 25 files across this repo that called `db.transaction((tx) => {...})` were surveyed (delegated to a research subagent first, to avoid burning context reading every file by hand, then spot-checked and the reads used to plan the actual fix) and put into three buckets: **(A) 10 files** whose "transaction" wrapped exactly one write statement (used only to read `.changes` uniformly) - the wrapper was simply removed, a lone statement already being atomic on both drivers. **(B) 12 files** whose writes were multiple, always-applied, mutually independent statements with no branching on an in-block read - converted to a new `runAtomicBatch(db, statements)` helper in `src/lib/db.ts`, which calls real D1's `.batch()` in production or the local better-sqlite3 driver's own native `.transaction()` in dev/test (both genuinely atomic on their own driver). **(C) the shared `receiving-sheet-lock.ts` helper plus `storage/move`/`storage/putaway`** looked like they needed a true intra-transaction read-then-write dependency, but on closer reading none actually did: every value the writes needed (batch find-or-create result, computed weights, aggregate totals) was fully decided by data already read, before any write - the apparent dependency was just an implementation artifact (an unnecessary re-select after insert, or a same-transaction read that didn't depend on anything else in the block). Rewritten to do all reads/validation first, then hand back a plan of unrun statements for the caller to batch. Also had to fix a related, smaller cross-driver landmine found the same way: real D1's `.run()` returns `{success, meta: {changes, ...}, results}`, not a flat `.changes` like better-sqlite3 - a new `changesOf(result)` helper in `db.ts` normalizes this everywhere instead of every route reading `.changes` directly (which would silently read `undefined` on D1). Full regression run after: `npx tsc --noEmit` clean, all 290 unit tests passing, all 75 E2E tests passing (Clerk stub mode, to route around the pre-existing PEN-030 sandbox limitation rather than conflate the two). See PEN-045 for the one disclosed residual risk this fix could not fully close. |
| PEN-046 | TASK-012 (Dashboard, SCREEN-001): several readings needed for the 10-panel aggregation, plus a real `getCurrentUser()` bug and a pre-existing unrelated E2E flake found while building/regression-testing it | RECORDED (Loop 47), not blocking - the full dashboard is built and tested; the bug found is fixed, the flake is disclosed not fixed (out of this task's own bounded scope) | Dashboard (all 10 panels) is live at `/dashboard`, reading `/api/dashboard` | **Design readings, none invented beyond what the locked sources actually state:** (1) Stock aging buckets (0-30/31-60/61-90/90+ days) are transcribed directly from SCREEN-001's own mockup example rows (`src/lib/business-rules/dashboard.ts`'s `stockAgeBucket`) - a different concept from Hold/Bulk's existing >3d/>7d thresholds (those track hold DURATION, not stock AGE), not a reuse of them. (2) `DISPATCHED`/`SCRAP` are the only `pallet_status` values excluded from the Stock Snapshot's "current stock" total - every other real status (including `IN_TRANSIT`) counts, since it is still owned inventory; not narrowed to only the mockup's 4 example status lines. (3) D-06 Transfers reads "Pending Receipt" as the same real count as "In-Transit" (`transfer_order_status` has no separate third state for "dispatched but not yet technically in transit"), the same reading PEN-041 already established for this entity. (4) "Role-based dashboard variants" (this task's own acceptance test) is read as real, matrix-grounded per-panel soft-restriction: the endpoint itself is ungated (no role has an explicit "view dashboard" permission anywhere in the real matrix, same PEN-022 precedent already applied to Bulk/Transfers/Maintenance/Locations), but the 3 panels that DO surface data the matrix explicitly gates elsewhere (Hold Tracking via `holds.view`, Stock Ledger recent + In-Out Summary via `stock.view_ledger`/`stock.view_summary`) are individually checked against the caller's real role via `getCurrentUser()`+`hasPermission()` and returned as `{restricted: true}` instead of leaking or hard-blocking the other 7 panels - not an invented per-role visibility scheme. (5) The refresh cadence (30s for every panel, D-01/D-04/D-08's own spec'd fastest interval) is one shared poll, not ten separate per-panel timers at their individually-spec'd 30s/60s intervals - a disclosed simplification, not a violation (a 60s-spec'd panel refreshing every 30s is strictly more current than spec'd). `static-guard`'s "lacks obvious auth guard" heuristic flags `src/app/api/dashboard/route.ts` for the same reason as the 7 already-accepted PEN-022 instances - see that item's own Loop 47 addition. **Real bug found and fixed, not just worked around:** `getCurrentUser()` (`src/lib/auth.ts`, built Loop 16, never actually called by any route until this task) calls Clerk's real `auth()` unconditionally - its own doc comment already promised "never throws", but in stub or partial Clerk config (`clerkMiddleware()` never ran) `auth()` itself throws Clerk's own internal "auth() was called but Clerk can't detect usage of clerkMiddleware()" error, not caught by the dashboard route's own defensive try/catch (which only expected `AuthNotConfiguredError`, wrongly assuming Loop 21's already-fixed failure mode). Caught during this loop's own full E2E regression (first real caller of `getCurrentUser()` ever exercised in stub mode through a browser) - the dashboard rendered its error state instead of real panels. Fixed at the source, not the caller: `getCurrentUser()` now checks `getClerkConfigStatus()` first and returns the same all-undefined "no session" shape for both `stub` and `partial` config, matching its own pre-existing contract exactly, instead of only for `configured`; the dashboard route's now-redundant defensive catch was removed as dead code rather than left in place. **Pre-existing, unrelated flake disclosed, not fixed (out of this task's bounded scope):** `tests/e2e/storage-putaway.spec.ts`'s "an assign attempt is honestly refused in Clerk stub mode" test times out at 30s, reproduced twice in a row, on code this task never touched (Storage/Putaway, TASK-005, Loop 24-37) - the immediately preceding test in the same file (a plain list read) already takes ~20s, consistent with general query slowness against this project's real, ever-growing shared local D1 file (PEN-026's own documented accumulation) rather than a logic defect in the assign mutation itself. Needs Alpesh's decision only if the dashboard's own 5 disclosed readings above should be different, and separately, whether the storage-putaway timeout is worth its own investigation loop. **Loop 48 addition:** the same `storage-putaway.spec.ts` file timed out again during TASK-013's own full regression - this time on the EARLIER test in the file (the plain "awaiting putaway" list read itself, not the assign mutation), on code this loop also never touched. A different test in the same file timing out each run, not the same one twice, is further evidence for the general-slowness diagnosis already given here over a logic defect in any one specific route. |
| PEN-047 | TASK-013 (FIFO Aging Report, SCREEN-013 / Flow 7): design readings for the new aging-by-batch + FIFO-compliance report, and a real permission-matrix tension found and deliberately NOT resolved either way | RECORDED (Loop 48), not blocking - the report is built and tested using the closest real readings; the permission tension is disclosed per the STOP RULE rather than guessed | `/stock/aging` (SCREEN-013) is live, reading `GET /api/stock/aging` | **Design readings, none invented beyond what the locked sources actually state:** (1) "FIFO pick suggestion in pick list generation" and "FIFO violation warning with override" (this task's own first two IN SCOPE bullets) were already fully built in Loop 41/TASK-008 (`assertFifoOrderOrOverride`, the oldest-first pick `<select>`, the conditional override-reason field) - re-verified, not re-built. (2) "Stock aging by batch" ages from each batch's own real `production_date` (the same FIFO-ordering field `assertFifoOrderOrOverride` already compares), not from pallet `created_at` - a different, more directly FIFO-relevant concept than TASK-012's own stock-snapshot aging (which ages from receipt, for a different question - "how long has this stock physically existed"). Reuses TASK-012's own 0-30/31-60/61-90/90+ bucket boundaries directly from `dashboard.ts` (a disclosed UI-consistency carryover, the same class of reuse `bulkAgeBucket` already established for Hold's thresholds) rather than inventing new ones. Scoped to currently-OK pallets only (the same dispatch-eligible pool `assertDispatchEligible` already draws from), not every batch ever received. (3) "FIFO compliance rate (% dispatches following FIFO)" (Section 15.2's own metric wording) is scoped to picks belonging to loading sheets that reached `DISPATCHED` specifically - the literal word "dispatches" - not every pick ever staged; a pick's own real `fifo_override_reason` column (null vs. not) is the compliance signal, no new field invented. With zero dispatched picks yet, `compliancePct` is `null`, not `0` or `100` - either number would misleadingly imply a real rate where none exists yet, a small but deliberate departure from `dashboard.ts`'s own zero-denominator convention (which uses `0`, appropriate there since a 0%-of-nothing bucket really is "0 cartons in this bucket", not "no data"). (4) No dedicated `stock.aging` permission exists anywhere in the real matrix - "Stock Aging (FIFO)" sits in the nav tree directly beside Stock Ledger/In-Out Summary (`stock.view_ledger`/`stock.view_summary`), so `GET /api/stock/aging` reuses `stock.view_ledger`, the closest real sibling screen's own permission, not an invented one. (5) The blueprint's own nav tree separately lists "FIFO Compliance" under a top-level "Reports" section - but the Stock landing page's own Loop 26/39 comment already establishes that this project's Reports section was deliberately folded into Stock's own screens rather than getting a separate `/reports` landing page (DSR Export already lives as a button on Stock Ledger/In-Out Summary, not its own screen) - followed that same already-established precedent rather than building a new, redundant `/reports` page for this one loop; SCREEN-013's own description ("Stock aging by batch, FIFO compliance") is one screen covering both nav-tree entries anyway. (6) FEFO (shelf-life-aware picking) is explicitly out of v1 scope per this task's own IN SCOPE bullet, and separately already marked DEFERRED in the architecture blueprint's own PEN-004 - nothing built for it, not even a disabled flag. **Real tension found, deliberately disclosed rather than resolved either way (STOP RULE):** the real permission matrix's own "FIFO Override" row grants only R03/R09/R12 - but the already-built, already-tested (GS-007, Loop 41) pick endpoint (`POST /api/loading-sheets/[id]/pallets`) lets whoever holds `loading_sheet.load` (R02 only) supply the override reason directly, with no separate `fifo.override` check at all. A code fix adding that check was written, then reverted before commit, once it was confirmed it would silently break the existing, passing GS-007 test (which has R02 both pick and override together) - changing already-tested, already-accepted golden-scenario behavior to match one possible reading of a matrix row, without asking, is exactly the guess the STOP RULE exists to prevent. Two real readings are both plausible and neither is guessed here: (a) the matrix's "FIFO Override" row is literally about this pick action, and R02 currently over-reaches - the code needs the fix that was reverted, and GS-007's own test fixture needs updating to R03/R09 for the override step; or (b) the matrix's row is about the earlier "decide what to dispatch" step (`dispatch_orders.generate_pick_list`, already R03/R09/R12-gated) - R03/R09 already decided the override is fine when generating the pick order, and R02 is only executing an already-authorized pick, so today's code is correct as-is. `static-guard`'s "raw SQL outside ORM" heuristic flags the new `src/app/api/stock/aging/route.ts` for the same false-positive reason PEN-042 already found and verified harmless on a different file - see that item's own Loop 48 addition. Needs Alpesh's decision on both: which FIFO-override reading is correct, and whether any of the 6 design readings above should be different. |
| PEN-048 | TASK-014 (Integration Tests & E2E): a systemic constraint on what Playwright can prove for any of the 20 locked negative tests in this sandbox, plus 2 new real gaps and 1 new real fix found while building the 10 golden-scenario / 20 negative-test E2E specs, and 3 real fixes to a CI pipeline that had never actually been run to completion | RECORDED (Loop 49), not blocking - all 10 golden scenarios and 20 negative tests have dedicated E2E coverage, using the strongest real proof actually available to each; the 2 new gaps are disclosed not silently invented around, the 1 new bug is fixed, the CI pipeline now runs the real scripts this repository actually has | `tests/e2e/golden-scenarios.spec.ts` (10 blocks) and `tests/e2e/negative-tests.spec.ts` (20 blocks) are live; `.github/workflows/ci.yml` now runs real migrate/seed/test/build/E2E steps | **Systemic finding, not a workaround:** every mutation route in this app calls `requirePermission`/`requireRole` before its own business-rule check, and in Clerk stub mode that always throws `AuthNotConfiguredError` (503) first, for any payload - so a real browser session can never observe a gated mutation's own specific 422/403/409 in this sandbox (PEN-030's own constraint, generalized here to its full scope for the first time). Of the 20 negative tests, most fall in this class - each one's own E2E block proves the honest 503 refusal is real (and that no fixture data mutated), and cross-references the exact `tests/unit/*-live.test.ts` line that proves the real specific error against the real route handler with only auth mocked. A minority are provable in full through E2E regardless of Clerk state: raw DB constraints/triggers (NS-006, NS-007, NS-014, NS-015 - proven directly against this app's own real local D1 file, bypassing the API entirely), a pure business-rule function call (NS-020), and a structural UI absence (also NS-020, and NS-006's own read-only detail page). **Real gap #1 (NS-004, structurally unreachable):** "Mix materials on one pallet" has no runtime 422 anywhere to observe - `pallets.material_id` is a single column and `receiving-sheet-lock.ts` (the only real pallet-creation path) always assigns a new pallet the sheet's own single material; no code path can even attempt to construct the invalid state the contract names. Disclosed via an explicit, reasoned `test.skip`, not a fabricated pass. **Real gap #2 (GS-009): RESOLVED (Loop 50)** - Alpesh: "Number 3 add karo...Ek pallet pe 2 batches ek hi material ki rakh sakte hai." Was: "Multiple Batches on Same Pallet" had no real creation path - `pallet_batches` is a real junction table (PEN-014's own translation) that structurally supports it, and the Rack Map's own "Mix of N batches" display (PEN-025) correctly renders it once it exists (proven by seeding it directly), but no route or UI flow in this app ever wrote a second `pallet_batches` row onto an already-existing pallet. Now built: `receiving-sheet-lock.ts` detects when a receiving-sheet row's `pallet_number` matches an already-real pallet IN THE SAME RESOLVED WAREHOUSE and adds a `pallet_batches` row (or increments it, if the same batch is reused again) instead of always creating a new pallet - gated by two real, already-locked signals, not invented ones: INV-006 (one pallet = one material - a material mismatch throws) and a real-receivability check on the existing pallet's own `status_code` (QC_HOLD/OK/HOLD/BULK only - DISPATCHED/IN_TRANSIT/REJECTED/SCRAP/CUSTOMER_SAMPLE/SAMPLE throw, since none of those describe a pallet still physically available to add cartons onto). INV-007's own weight-limit check (NS-005/this item's own Real fix, above) now checks the pallet's cumulative new total for a reused pallet, not just one row's own weight. A same-numbered pallet in a DIFFERENT warehouse is deliberately NOT treated as a match - no field anywhere scopes pallet numbers to a warehouse, so that case is left exactly as it already behaved before this change, a disclosed limitation not a new decision. **Real, unrelated bug found and fixed while building this:** building real reuse-detection surfaced that this test suite's own long-lived fixture pallet numbers (`tests/unit/receiving-sheet-live.test.ts`'s "30673"/"30676", the same plain example numbers the canonical flow document/architecture blueprint use, also reused as fixtures by several other test files) were never actually collision-safe in this shared, ever-growing local D1 file (PEN-026) - before this loop, a re-run silently created a harmless second `pallets` row with the same number; after this loop's own real reuse logic, a second run instead accumulated real cartons onto the SAME real pallet until it broke the material's own weight limit (reproduced, not hypothetical - this loop's own first test run of the pre-existing fixture failed exactly this way, against a real cross-file "30673" collision first, then against its own re-run). Fixed by giving every pallet-number fixture in that file a fresh per-process-run suffix, the same uniqueness answer PEN-026 already established for undeletable ledger-referenced rows. Proven end-to-end (not just at the seeded-display level GS-009's own E2E block already had): `tests/unit/receiving-sheet-live.test.ts`'s new "reusing an existing pallet number adds a second batch (GS-009)" block locks two real Receiving Sheets (different batches, same material, same pallet number) through the real API routes against real local D1, and asserts one real pallet row, two real `pallet_batches` rows, correct accumulated `total_cartons`/`total_weight_kg`, a correct running-balance `stock_ledger` row, the INV-006 material-mismatch refusal, and the non-receivable-status refusal. **Real fix (NS-005, INV-007):** this task's own audit found "Pallet weight exceeds limit" was never enforced anywhere - `materials.palletWeightLimitKg` (PEN-007) was stored and editable but never compared against anything. Fixed at the one real point a pallet's weight is decided: `receiving-sheet-lock.ts` now refuses to lock a sheet if any row's resulting pallet would exceed its material's own real limit, with a new live-DB test (`tests/unit/receiving-sheet-live.test.ts`) proving the 422, no pallet written, and the sheet left un-transitioned. **CI pipeline, never actually run to completion until this loop:** `.github/workflows/ci.yml` referenced 4 npm scripts that were never defined (`test:invariants`, `test:negative`, `dev:test`, `test:e2e:negative`) - every job past `harness-preflight` would have failed on its very first missing-script step on any real push/PR to this branch. Fixed: `application-quality`/`browser-e2e` now run the real `npm run test`/`npm run test:e2e` (this repository has no real file-level invariant/negative split to alias - inventing hollow aliases for the old names would have been more misleading than removing them); both jobs now also run `npx wrangler d1 migrations apply DB --local` + `npm run db:seed` first (a fresh CI checkout has no local D1 file at all - every `*-live.test.ts` file and most E2E specs need the real seeded LIMBASI warehouse/materials to exist, a real, separate gap this loop also found while fixing the rest). One deliberate inefficiency, not silently smoothed over: the `browser-e2e` job's own "Wait for app" health-check step (a `curl`-polling loop against port 3000) could not be edited or removed at all through this session's own tool-mediated writes - every attempt to touch that one step, even a trivial one, was refused the same way this project's protected-path writes always are (PEN-017's own precedent, extended here to a specific CI step rather than a whole file). Rather than fight that protection, a real `npm run dev:test` script (`next build && next start -p 3000`) and a real "Start test app" step were added back in front of it, so the existing, unmodified health-check gate has a real server to observe - the actual E2E tests then still run via `npm run test:e2e`, which builds and starts its own second copy of the app on port 3100 through Playwright's own already-established `webServer` config. The app now builds twice in this one job; a real, disclosed cost of respecting a write-protection this session could not get past, not an oversight. Needs Alpesh's decision only on: which reading of the FIFO-override permission tension (already recorded in PEN-047) is correct, whether NS-004/GS-009's own real gaps are worth building real enforcement/creation for, and whether the CI job's now-doubled build is worth a human-applied edit to that one protected step (e.g. removing it entirely, now that `test:e2e` is self-sufficient). |
| PEN-045 | The "guard update, then batch the dependent writes" pattern (PEN-044's fix for routes whose original `db.transaction()` checked `if (result.changes === 0) return 0` before proceeding - transfer-orders/receive & dispatch, loading-sheets/dispatch, receiving-sheets/confirm-warehouse & confirm-packing) runs as two separate network calls instead of one real atomic unit | **DISCLOSED, ACCEPTED (Loop 46)** - not blocking, no fix exists within D1's real capabilities without a materially larger rewrite | Same five routes named above; nowhere else | D1's `batch()` executes all given statements regardless of whether an earlier statement in the same batch affected 0 rows (a 0-row UPDATE is not a SQL error, so batch() would not abort the rest) - there is no way to make a later statement in one batch conditional on an earlier one's row-count within that same atomic call. The only way to preserve the "don't touch anything else unless the guard actually matched" invariant these routes depend on is to run the guarded UPDATE alone first, check its result, and only then run the dependent writes as their own separate atomic batch. This means a crash or network failure in the narrow window between those two calls could leave (e.g.) a transfer order marked IN_TRANSIT/RECEIVED with its pallets not yet moved, or a receiving sheet's guard applied without its materialization rows - an inconsistency the single real ACID transaction this replaced could never produce. This is strictly narrower than the alternative (batching everything together and risking the guard-failed-but-pallets-moved-anyway corruption a naive full-batch would allow), and is the same class of unavoidable platform gap already accepted when Alpesh chose the Cloudflare Workers/batch() path over a REST-API workaround (that earlier decision assumed `batch()` alone would give full transaction parity - PEN-044/045 together are the honest correction that D1 has no primitive that does, for any route needing a guard). A true fix would mean pushing the guard condition into each dependent statement's own WHERE clause (e.g. `WHERE EXISTS (SELECT 1 FROM transfer_orders WHERE id=? AND status=?)`, since D1's batch() does execute its statements sequentially within one real transaction, so a later statement CAN see an earlier one's effect) and turning every ledger `INSERT ... VALUES` into a conditional `INSERT ... SELECT ... WHERE EXISTS (...)` - doable, but a materially larger, more error-prone rewrite than this loop's scope justified given the residual risk is a rare crash-in-a-microseconds-wide-window, not a routine one. Needs Alpesh's decision only if that fuller rewrite is wanted for these 5 routes specifically. |

## IMPORTANT
| ID | Item | Status | Notes |
|----|------|--------|-------|
| - | Technology stack | RESOLVED | Cloudflare D1 + Clerk approved 2026-09-13 |
| - | Browser/E2E verification of implemented scope (Loop 18) | RESOLVED for current scope | Playwright added as a devDependency only; tests/e2e/app-shell.spec.ts (9 tests, all passing) verifies mobile-first behavior, desktop sidebar behavior, the Clerk stub-mode auth boundary on /sign-in and /sign-up, and Next.js's 404 error state, against a real local production build. Full detail in docs/PROGRESS.md's Loop 18 evidence section. Cannot yet cover forms, validation, or real database interaction from the browser, since no CRUD screen exists - that remains blocked by PEN-014, not by anything in this loop. |

## DEFERRED
| ID | Item | Notes |
|----|------|-------|
| PEN-002 | SAP deep integration (two-way sync) | v1 uses SAP codes as reference only |
| PEN-003 | Barcode/QR scanning | v1 manual entry — model supports scanner later |
| PEN-004 | Shelf life / FEFO | FIFO in v1, FEFO configurable later |
| PEN-005 | Photo attachments for receiving | v1 uses remarks — R2 ready for photos later |
| PEN-006 | Offline sync conflict resolution | PWA deferred — D1 + Cloudflare edge is fast enough for v1 |
