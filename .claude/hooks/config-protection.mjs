#!/usr/bin/env node
process.stdout.write(JSON.stringify({decision:'block',reason:'MONARCH HARNESS: project Claude Code hook settings are protected. Change them manually with explicit human approval.'}));
